# Optionnel : active la lecture des fichiers Excel (.xlsx, .xlsm),
# PowerPoint (.pptx) et OpenDocument (.odt, .ods, .odp). Sans ce paquet, les
# autres formats continuent de fonctionner normalement.
openpyxl>=3.1.0
python-pptx>=0.6.23
odfpy>=1.4.1
