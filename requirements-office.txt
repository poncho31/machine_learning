# Optionnel : active la lecture des fichiers Excel (.xlsx, .xlsm) et
# PowerPoint (.pptx). Sans ce paquet, les autres formats continuent de
# fonctionner normalement.
openpyxl>=3.1.0
python-pptx>=0.6.23
